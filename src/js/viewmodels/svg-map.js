/** @module */
define([], function () {
	'use strict';

	/**
	 * Svg map
	 *    used in the wield and well map sections
	 * @constructor
	 */
	var exports = function () {
		/**
		 * A standard ratio for the graph
		 * @type {number}
		 */
		this.ratio = 1 / 2;
	};

	return exports;
});
